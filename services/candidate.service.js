const httpStatus = require("http-status");
const ApiError = require("../middlewares/ApiError");
const { Candidate } = require("../models");

const createCandidate = async(candidateBody) => {};

const deleteCandidate = async(candidateBody) => {};

const updateCandidate = async(candidateBody) => {};

const queryCandidates = async() => {};

const getCandidateByCandidateId = async(candidateBody) => {};

module.exports = {
    createCandidate,
    deleteCandidate,
    updateCandidate,
    queryCandidates,
    getCandidateByCandidateId
};